#!/bin/bash

# Development secrets - much simpler than production
RAILS_MASTER_KEY=$(cat config/master.key 2>/dev/null || echo "development_key_placeholder")

# Simple registry password for local development
KAMAL_REGISTRY_PASSWORD="dev"
